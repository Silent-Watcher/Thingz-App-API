import { model, Schema } from 'mongoose'; const
{{name}}Schema = new Schema({ name: { type: String, required: true }, }); const
{{name}}Model = model('{{name}}',{{name}}Schema); export default
{{name}}Model;